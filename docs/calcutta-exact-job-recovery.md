# Exact Calcutta job activation recovery

Owner-authorized bounded recovery for the never-started 2026 job `83ed6e87-1a2c-4100-a8d6-9480621c7e8e`, auction 37 / published revision 41, config 2, $18,500 and no calculated result.

The incremental SQL removes only `pg_catalog.` from the installed claim function's single LEAST and GREATEST expressions. It preserves the exact installed function and its normal current-activation guard. Installation does not process or carry any job.

The new protected operator function is not a public RPC and grants no execution to anonymous, authenticated or service roles. It requires the existing verified Director authority, active protected release plan, canonical release rebind, exact job hash, never-started state and unchanged canonical financial/source fingerprints. The only job field it changes is activation_revision. The current activation is independently derived. A unique immutable receipt records old/new activation and pre/post hashes. Repeated valid recovery returns the existing receipt.

## Certification

The isolated PostgreSQL integration copies canonical Production inputs into local PostgreSQL. It exercises the installed claim, calculation engine, completion and exact frozen participant read. External deployment checks are fixture seams; production activation, Director, job and financial checks are retained. Runtime binding is separately certified during protected deployment.

Run with `BAGGER_CALCUTTA_RECOVERY_FIXTURE` pointing to a private audit directory containing the read-only canonical exports. Financial fixtures are deliberately not committed. The certification produced 27 denial checks, 10 lease checks, 17 concurrency checks, immutable-receipt checks and a successful provisional 24-golfer/24-portfolio, zero-completed-round result. Both local and rollback-only hosted isolated participant payloads pass unchanged Build 10's decoder and presenter guards.

The hosted test operates on isolated schema copies and rolls back. It does not claim the real job. The real operation remains gated by protected staging, signed promotion/rebind, fresh exact-job preflight, carry-forward readback, normal processing and authenticated live participant acceptance.

Application/participant/security regressions: 513 passed. Release 133 runtime byte-preservation checks: 2 passed. Production build passed. Existing single-entry-clear PostgreSQL regression passed. Existing round-control PostgreSQL acceptance passed. The obsolete Calcutta candidate-integrity test pins an earlier candidate SHA and is not applicable; it was not weakened. Broader concurrent PostgreSQL runs hit host shared-memory capacity; focused isolated reruns passed.

No native, PWA, authentication, scoring, auction entry, publication, Net Skins or Odds behavior is changed. No replacement calculation job is created.
