# Step 12 Production Cutover Runbook v2

Status: certification artifact only. This document does not authorize Step 12.

## Fixed production resources

- Supabase project: `ymqhhtxaywtqllynrmxe`
- Google workbook: `1umqPxiQxN9_jwmsD7IcVTzqxPmMycYLlrY_gm31l5U4`
- Canonical domain: `https://baggerinv.com`
- Tournament: `2026`
- Odds publication authority: `GOOGLE`

The frozen release SHA, certification fingerprint, activation revision,
authority generation, admission generation, and provider-fence evidence are
generated by Step 11.6. They must never be filled from memory.

## Non-negotiable writer invariant

Canonical scoring authority and mutation admission are separate facts. The only
supported forward states are:

```text
GOOGLE canonical / legacy admission OPEN
  -> GOOGLE canonical / legacy admission CLOSING
  -> GOOGLE canonical / legacy admission CLOSED
  -> GOOGLE canonical / Supabase epoch PREPARED / both ingresses paused
  -> SUPABASE canonical / legacy admission CLOSED
```

`GOOGLE admission OPEN + SUPABASE ingress OPEN` is forbidden. A scoring pause
is acceptable; ambiguous or dual authority is not.

## Required provider fence

Before the close operation is eligible, the operator must prove that every
Production-capable origin is controlled. The proof must bind:

- Vercel project, deployment ID, Git SHA, environment, aliases, and immutable
  deployment hostnames;
- the exact credential generation available to the new barrier-aware release;
- revocation or removal of every legacy credential/workbook permission that can
  bypass the v2 admission contract;
- the Production workbook permission fingerprint;
- the request-derived host/deployment enforcement fingerprint; and
- zero Preview credentials or resources.

A redirect, DNS promotion, application-local check, or database attestation by
itself is not provider enforcement. Old deployments must be cryptographically
unable to write, or forced through a sole credential-owning barrier-aware
service. The provider proof contains hashes and non-secret identifiers only.

Provider-fence evidence is deliberately short lived. If it approaches expiry
while the gate is `PAUSED` and the active closure is `CLOSING` or `CLOSED`, call
`refresh_production_scoring_external_fence_evidence` with a durable request ID,
the prior evidence ID, active closure, exact optimistic revisions/generations,
and fresh provider readback. The provider, deployment, credential, and writer
coverage fingerprints must remain byte-for-byte identical to the evidence
already bound to the closure. Only the reconciled legacy lease-set evidence and
capture time may advance. Record the returned evidence ID and new revisions;
never substitute a looser or operator-asserted scope.

## Forward phase sequence

For every phase, recapture state, verify the prior receipt, record the new
activation/admission revision and generations, and classify rollback before
advancing.

1. `DORMANT -> STAGED`
   - Stage the exact frozen release.
   - Google scoring and Passport identity remain canonical.
   - Legacy admission remains `OPEN`.

2. `STAGED -> STATIC_BACKEND`
   - Deploy the barrier-aware candidate with legacy authority preserved.
   - Verify exact Production resources and the complete origin/credential
     matrix.
   - The candidate must fail closed when admission cannot be verified.

3. `STATIC_BACKEND -> READ_CUTOVER -> IDENTITY -> CURRENT_READS`
   - Follow the separately certified read and identity transitions.
   - Google remains canonical scoring authority.
   - Legacy admission may remain `OPEN`.

4. Close legacy admission atomically
   - Call `close_production_scoring_admission` with the current activation,
     authority, admission revisions/generations, provider-fence evidence ID,
     exact resource tuple, deployment ID/SHA, and durable request identity.
   - One transaction changes admission `OPEN -> CLOSING`, pauses execution,
     records a lease high-watermark, and prevents new v2 admissions.
   - The close boundary serializes with an admitted canonical transaction. If
     admission won first, close waits for that canonical database transaction;
     durable outbox/archive work created by the transaction remains visible and
     still has to drain. A returned close receipt does not imply that durable
     queue work is delivered or verified.
   - Lost responses are resolved by `inspect_production_scoring_admission`; do
     not infer failure and do not issue a reopen.

5. Drain admitted legacy writers
   - Call `drain_production_scoring_admission` and inspect the protected
     diagnostics.
   - `ADMITTED`, `WRITE_STARTED`, `AMBIGUOUS`, `PARTIAL_WRITE`, and
     `LEGACY_UNCLASSIFIED` all block closure.
   - Expiry never proves no write. An external timeout or lost response remains
     unresolved until Google readback/idempotency proves the outcome.
   - Require active/potential writers `0`, ambiguous writers `0`, partial
     writers `0`, and unresolved legacy writers `0`.
   - Require unresolved Google outbox events `0` and unresolved scorecard
     archive jobs `0` before closure finalization or authority prepare/commit.

6. Seal the closed Google boundary
   - Capture fresh Google readback after admission is already `CLOSING` and the
     drain set is empty.
   - Compare it with the Production Supabase shadow and reconcile through the
     supported import path while Google remains canonical.
   - Call `finalize_production_scoring_admission` with the stable final Google
     fingerprint, exact match revision/checkpoint maps, queue state, provider
     evidence, and closure high-watermark.
   - Require admission `CLOSED`, zero possible writers, stable repeated
     readback, zero unresolved outbox/archive work, and
     `FINAL_GOOGLE_AUTHORITY_SNAPSHOT_SAFE=true`.

7. Prepare Supabase authority
   - Call `prepare_production_authority_epoch(CUTOVER)` only when
     `SUPABASE_AUTHORITY_PREPARE_SAFE=true`.
   - Bind the immutable closure, final Google fingerprint, shadow fingerprint,
     provider-fence evidence, activation/admission revisions and generations,
     deployment SHA, and exact Production resources.
   - Supabase scoring ingress remains paused and workers remain disabled.

8. Deploy precommit scoring configuration
   - Application configuration expects Supabase, but database ingress remains
     paused.
   - New scoring returns a clear temporary-pause response. No legacy request is
     reinterpreted as a Supabase mutation.

9. Final pre-write check v2
   - Exact frozen SHA and Production project/workbook/domain.
   - Expected phase, authority generation, activation revision, and prepared
     epoch.
   - Legacy admission `CLOSED`.
   - Active/potential/ambiguous/partial legacy writers all `0`.
   - Provider fence current and all Production-capable origins controlled.
   - Final Google fingerprint stable and Supabase shadow exact.
   - Supabase ingress paused; workers disabled.
   - Participant identity and stale-PWA checks healthy.
   - First Supabase canonical write possible `false`; observed `false`.

10. Commit Supabase authority
    - Call `commit_production_authority_epoch` only when
      `SUPABASE_AUTHORITY_COMMIT_SAFE=true`.
    - The transaction rechecks the immutable closure, fingerprints, provider
      fence, revisions, generations, zero writer set, and precommit ingress.
    - Supabase becomes sole canonical scoring authority; legacy admission stays
      `CLOSED`.
    - Record authority epoch, activation revision, commit timestamp, and first
      write markers separately.

11. `WORKERS -> ODDS_WAR_ROOM -> OBSERVATION`
    - Enable only certified mirror/archive workers after Supabase authority is
      healthy.
    - Google mirror/archive and retained authoring use explicit non-canonical
      writer intents.
    - Odds inputs may move to Supabase; Odds publication remains Google.

## Machine-checkable safety predicates

`FINAL_GOOGLE_AUTHORITY_SNAPSHOT_SAFE` requires:

- authority `GOOGLE` and admission `CLOSED`;
- exact Production project/workbook/tournament;
- active, potential, ambiguous, partial, and legacy-unclassified writers `0`;
- current provider-fence evidence;
- no conflicting prepared epoch;
- zero unresolved canonical queue work; and
- stable Google readback bound to the current revisions/generations.

`SUPABASE_AUTHORITY_PREPARE_SAFE` additionally requires an immutable final
Google fingerprint and exact shadow parity.

`SUPABASE_AUTHORITY_COMMIT_SAFE` additionally requires the valid prepared
epoch, unchanged closure/provider/fingerprints, paused Supabase ingress, exact
precommit deployment configuration, and first-write markers still false.

`LEGACY_GOOGLE_REOPEN_SAFE` is false while Supabase is canonical, a prepared
epoch exists, any Supabase-window write is unreconciled, rollback is incomplete,
or a newer operator revision has advanced the transition.

## Failure and idempotency rules

- Duplicate close/finalize/reopen requests use durable request identities and
  return the authoritative existing result or a stale-revision rejection.
- A lost close response never triggers a reopen. Inspect state first.
- A failed/ambiguous Google transport blocks closure until explicit readback
  resolves it.
- Aborting a prepared epoch leaves Google canonical with admission `CLOSED`.
  Reopen is a separate audited operation.
- Do not automatically reopen after a provider, fingerprint, parity, deployment,
  or Supabase failure.

## Rollback matrix

| State | Supabase authority | Supabase write observed | Legacy admission | Safe rollback |
|---|---:|---:|---|---|
| Before close | No | No | OPEN | Normal legacy operation |
| CLOSING | No | No | CLOSING | Resolve/drain, abort, then explicit safe reopen |
| CLOSED pre-prepare | No | No | CLOSED | Explicit safe reopen |
| Prepared | No | No | CLOSED | Abort epoch, then explicit safe reopen |
| Committed/no write | Yes | No | CLOSED | Pause Supabase, roll back authority, then reopen |
| Committed/post-write | Yes | Yes | CLOSED | Pause, enumerate/reconcile, roll back authority, then reopen |

Never reopen Google before authority is safely Google again. Unexpected Google
divergence after commit is a reconciliation incident even when no Supabase write
has been observed.

### Supported post-commit rollback sequence

Rollback uses a second admission closure; it does not reuse or reopen the
consumed legacy closure and it does not flip source configuration first.

1. Call `close_production_scoring_admission` with
   `expected_authority=SUPABASE`. In one transaction the Supabase ingress gate
   becomes `PAUSED` and a `SUPABASE_INGRESS` closure is created. Legacy Google
   admission remains `CLOSED`.
2. Call `drain_production_scoring_admission` for that exact closure. A racing
   Supabase mutation is serialized by the same advisory lock and remains in the
   drain set until its transaction is definitively resolved.
3. Enumerate the complete Supabase authority-window write set. For post-write
   rollback, reconcile it into the Google rollback target and require
   unresolved, lost, and duplicate counts all equal `0`.
4. Call `finalize_production_scoring_admission` for the
   `SUPABASE_INGRESS` closure with the stable reconciled fingerprints and exact
   revisions/checkpoints. The execution gate remains `PAUSED`.
5. Call `prepare_production_authority_epoch(ROLLBACK)` using that closed
   Supabase-ingress closure, then `commit_production_authority_epoch` for the
   prepared rollback epoch.
6. Only after the rollback commit proves Google canonical and Supabase ingress
   unavailable may `reopen_production_scoring_admission` create a fresh legacy
   admission generation. Restore provider capability only as part of that same
   certified rollback sequence.

A post-commit/no-write rollback follows the same ordering but has an empty
Supabase authority-window mutation set. There is no supported direct transition
from `SUPABASE canonical` to `GOOGLE admission OPEN`.

## Cutover-pause client behavior

During `CLOSING`, `CLOSED`, and prepared precommit states, participant scoring
must fail with a recoverable response equivalent to:

> Scoring is temporarily paused while tournament state is updated. Refresh shortly.

The client must not queue an unbounded retry, silently select another authority,
or replay a legacy mutation after Supabase commits.

## Event-dependent checkpoints

Do not fabricate a score, Finalize, Reopen, pairing, or Odds publication. If the
tournament remains pre-tournament, arm the first-score and first-Finalize
checkpoints for the first legitimate events.

## Authorization boundary

Step 11.6 may install and certify this dormant contract but must end with
Production `DORMANT`, Google scoring, Passport identity, legacy admission
`OPEN`, Supabase ingress/workers disabled, and no Supabase canonical write.

Executing this runbook requires a new, explicit Step 12 owner authorization.
